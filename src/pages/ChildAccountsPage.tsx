import EntryLayout from "../components/layouts/EntryLayout";
import Button from "../components/ui/button/Button";

const ChildAccountsPage = () => {
  return (
    <EntryLayout>
      <Button>Add a new child user +</Button>
    </EntryLayout>
  );
};

export default ChildAccountsPage;
